
## Cron PATH 问题已修复
- 定时任务 `openclaw:auto-update-with-sidecar-happy-path` 原本失败（找不到 `openclaw` 命令）
- 已改为使用完整路径：`/Users/kevinzhang/.npm-global/bin/openclaw`
- 删除了 `npx` 方案，直接用绝对路径更稳
- MiniMax M2.5 监控任务正常运行中，目前仍显示 "not allowed"

## 用户偏好：网络搜索必须走 MCP
- 用户明确要求：网络搜索用 mcp，不要用 web_fetch
- 当前已配置 MiniMax MCP（用于编程/本地搜索）和 qmd MCP（用于本地文档搜索）
- MiniMax MCP 的 web_search 功能之前显示 API key 无效，需排查

## Cron PATH 问题已修复
- 已在 ~/bin 做软链接指向 npm global openclaw
- cron 任务已添加 PATH 环境变量
- MiniMax M2.5 监控任务持续运行中，目前仍显示 "not allowed"

## MCP 网络搜索状态更新
- 用户强调：网络搜索优先走 MCP
- 已实测 `mcporter call MiniMax.web_search` 成功（status_code: 0）
- 结论：MiniMax MCP 的 web_search 当前可用，后续默认使用 MCP 进行网络搜索
